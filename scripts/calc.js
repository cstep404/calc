// operation functions
const add = (a, b) => a + b;

const subtract = (a, b) => a - b;

const multiply = (a, b) => a * b;

const divide = (a, b) => a / b;

// operator function - takes an operator (+, -, *, /) and two numbers (a and b)
const operate = (a, operator, b) => a + operator + b;

// variables
let initialNumber;
let operator = "";
let nextNumber;


